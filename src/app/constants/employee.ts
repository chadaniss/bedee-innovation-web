export interface IEmployee {
  _id: string;
  employeeId: string;
  firstName: string;
  lastName: string;
  department: string;
  activeDate: string;
  activeTime: string;
}

export interface IMappedUserData {
  name: string;
  id: string;
  department: string;
}